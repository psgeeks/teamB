# 잘못된 이해로 빚어진 자료구조 설계

처음에 `letencyList[][]` 배열을 만들어 a 정점에서 b 정점까지의 거리를 모두 저장했었음

그래서 a->b까지의 거리를 O(1)으로 탐색할 수 있게 구현했었음

`measure()`에 대한 리턴은 완벽했음.

`test()`도 `mDevice`를 기준으로 가장 먼 정점까지의 거리 2개의 합으로 반환하려 했음.

이건 가장 먼 정점까지의 거리 2개가 동일한 간선을 지나는 경우를 고려하지 않았음.

그래서 기존의 코드 밀어 버리고 다시 만듬