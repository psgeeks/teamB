# 신소재케이블2 실수 로그

## visited의 누락
- 매 dfs마다 visited를 넣어줘야 하는데 이를 고려 못함 -> 무한루프 발생
- 시작 노드를 true로 해줘야 하는데 이를 하지 못함 -> 시작 노드 재방문 하는 경우 발생

## test의 디버깅
- 시작 노드에서 연결된 노드들로부터 dfs를 시작했으면 해당 연결된 노드들을 방문처리 했어야 하는데 이를 못했었음.
- 마찬가지로 시작 노드와 연결된 노드 간에 거리를 최초에 더해줬어야 했는데 이를 고려 못함.